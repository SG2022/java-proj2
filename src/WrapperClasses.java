import java.util.*;

public class WrapperClasses {
  public static void main(String[] args) {
    /*
     * convert string tp primitive and wrapper data types
     * int x =Integer.parseInt("10");
     * Integer i =Integer.valueOf("12");
     * float f = Float.parseFloat("4.0f");
     * f=Float.valueOf(f);
     * double d = Double.parseDouble("4.5d");
     * System.out.println(f);
     * int i1 = Integer.valueOf("20");//returns Integer and unboxes to primitive int
     * Integer.max(2,4);
     * 
     * // all functions of a wrapper class
     * Integer int1 = Integer.parseInt("200");
     * System.out.println(Integer.MAX_VALUE);
     * System.out.println(Integer.MIN_VALUE);
     * System.out.println(int1);
     */
    // -------------------------------------------------------------------------------------

    /*
     * //examples of autoboxing
     * List<Double> weights = new ArrayList<>();
     * weights.add(50.0d);//autoboxes double into wrapper class Double object
     * weights.add(new Double(40.5D));//explicit creating a wrapper class Double
     * object
     * weights.remove(50.7d); //autoboxes to wrapper object and then remove it
     * double d =weights.get(0); //retrieve Double object and unbox into double
     * primitive
     * 
     * List<Integer> heights = new ArrayList<>();
     * heights.add(null);
     * int h = heights.get(0); //null pointer exception
     */

    /*
     * Scanner sc = new Scanner(System.in);
     * System.out.println("enter your name");
     * String name= sc.nextLine();
     * System.out.println("enter your age");
     * int age = sc.nextInt();
     * int marks =Integer.parseInt(System.console().readLine("enter your marks"));
     * System.out.println("Name is  " + name);
     * System.out.println("Age is "+ age);
     */

  }

}
